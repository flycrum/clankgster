# Step 5 — Return setup summary (shared)

Return:

* resolved `target_plugin_path`
* created files and folders
* optional doc-bot closer: offer to run the first `/rawdocs-struct-sync` to observe outputs
