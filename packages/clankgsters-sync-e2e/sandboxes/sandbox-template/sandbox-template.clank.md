## Purpose

Template project copied into `.tests/current` for each e2e case.

## Conventions

- Root context uses `CLANK.md` (and nested `packages/app/CLANK.md`).
- Source marketplace fixtures live under `.clank/plugins/` (root + nested).
- Source skills fixtures live under `.clank/skills/`.
