# Sandbox agents

Root CLANK.md for e2e test sandbox. Sync will create CLAUDE.md symlink here when claude is enabled.

This file is used by the e2e test harness to simulate a real repo with plugins under `.clank/plugins` (root and nested).
