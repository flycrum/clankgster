/** Example local override template; remove `.example` from the filename to activate as repo-root `clankgsters.local.config.ts`. */
const clankgstersLocal = {
  loggingEnabled: true,
  agents: {
    claude: true,
    cursor: false,
    codex: false,
  },
};

export default clankgstersLocal;
